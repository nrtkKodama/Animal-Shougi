{
  "name": "dobutsu-shogi-server",
  "version": "1.0.0",
  "description": "Server for Dobutsu Shogi online play",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=automatic",
    "start": "npm run build && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "immer": "^10.1.1",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}